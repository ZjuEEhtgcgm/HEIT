<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.versionone">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".p_i_Activity" />
        <activity android:name=".chat_Activity" />
        <activity android:name=".market_info_Activity" />
        <activity android:name=".application_general" />
        <activity android:name=".call_office_Activity" />
        <activity android:name=".chat_history_Activity" />
        <activity android:name=".manage_Activity" />
        <activity android:name=".application_fix_Activity" />
        <activity android:name=".energe_net_Activity" />
        <activity android:name=".safe_Activity" />
        <activity android:name=".improve_Activity" />
        <activity android:name=".contact_manage_Activity" />
        <activity android:name=".contact_information_Activity" />
        <activity android:name=".Login_Activity" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".contact_Activity"></activity>
    </application>

</manifest>